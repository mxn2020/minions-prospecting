{
    "name": "minions-prospecting-workspace",
    "private": true,
    "version": "0.1.0",
    "description": "Web search configs, prospect discovery rules, and lead scoring criteria",
    "type": "module",
    "license": "MIT",
    "author": "Mehdi Nabhani <mehdi@the-mehdi.com> (https://the-mehdi.com)",
    "workspaces": [
        "packages/*",
        "apps/*"
    ],
    "scripts": {
        "build": "pnpm --filter @minions-prospecting/sdk run build && pnpm --filter @minions-prospecting/cli run build && pnpm --filter @minions-prospecting/docs run build && pnpm --filter @minions-prospecting/blog run build && pnpm --filter @minions-prospecting/web run build",
        "deploy:blog": "pnpm --filter @minions-prospecting/blog run build && netlify deploy --dir apps/blog/dist --prod",
        "deploy:docs": "pnpm --filter @minions-prospecting/docs run build && netlify deploy --dir apps/docs/dist --prod",
        "deploy:web": "pnpm --filter @minions-prospecting/web run build && netlify deploy --dir apps/web/dist --prod",
        "test": "pnpm --filter @minions-prospecting/sdk run test",
        "lint": "pnpm --filter @minions-prospecting/sdk run build && pnpm --filter @minions-prospecting/sdk run lint && pnpm --filter @minions-prospecting/cli run lint",
        "deploy": "pnpm run deploy:blog"
    },
    "engines": {
        "node": ">=20"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}